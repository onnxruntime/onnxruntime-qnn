# Usage
Please refer to the command for qualcomm internal git hooks
```bash
# Enable git hooks
git config --local core.hooksPath ./hooks_qcom
# Disable git hooks
git config --local --unset core.hooksPath
```